# 工作交接 — 2026-08-11 晚：接入 hermes-agent-ultra 引擎真实能力

> 给下一位接手 AI 看的交接文档。本文件描述今晚（2026-08-11）在 `C:\Users\Administrator\my-hermes-rs`
> （React 19 + Vite + Tailwind v4 + Tauri v2 + Rust 桌面客户端）做的全部改动、架构、验证状态与注意事项。
> 后端引擎：`D:\hermes-agent-ultra-main`（Rust workspace）。原型参考：`D:\hermes-agent-main`（Python，Electron 前端）。

---

## 0. 一句话总结

把之前因"需引擎支持"而搁置的 4 类功能全部接上了真实引擎：
**① 斜杠命令面+认证（hermes-http 8787）② 定时任务（api_server 8090 /api/jobs）③ 会话列表/搜索（直读本地 sessions.db FTS5）④ ACP 边车细粒度流式（spawn `hermes acp start`，thinking/tool 增量）**。
全部带"引擎不可达 → UI 降级提示"兜底，不白屏。

## 1. 架构：四路引擎接入

```
┌─────────────┐  Tauri IPC (invoke)  ┌──────────────────┐  HTTP   ┌────────────────────────────┐
│  UI (React) │ ───────────────────► │ Tauri Relay (Rust)│ ──────► │ hermes-http (引擎, 8787)    │
│             │ ◄──── 事件/Channel ── │  relay.rs          │ ◄────── │ 对话/命令面/认证/RPC         │
│             │                      │  relay_cron.rs     │ ──────► │ api_server (8090) 定时任务   │
│             │                      │  sessions.rs       │ ────    │ {hermesHome}/sessions.db    │
│             │                      │  acp.rs (边车)      │ ──────► │ hermes acp start (stdio)    │
└─────────────┘                      └──────────────────┘          └────────────────────────────┘
```

- 前端不直连引擎，全走 Rust Relay 命令（`src/lib/api/client.ts` 的 `RealClient` invoke）。
- 前端抽象：`HermesClient`（`MockClient` / `RealClient`），开关 `VITE_MOCK=1`（默认 mock）/ `0`（真实）。
- 流式用 **Tauri Channel**（`new Channel<T>()` + `invoke(..., { ch })`），参考 zosma-cowork 模式。

## 2. 今晚改动明细

### WS-A 命令面 + 认证（hermes-http 8787）

**Rust `src-tauri/src/relay.rs`** 新增：
| 命令 | 引擎调用 | 说明 |
|---|---|---|
| `relay_command(sessionId, command)` | `POST {engine}/v1/commands`，请求体 `{"command":"/usage","session_id":...}` | 响应 `{accepted, output}`；命令无 `/` 自动补 |
| `relay_auth_status()` | `GET {engine}/auth/status` | 原样透传；不可达给 `{reachable:false}` |
| `relay_auth_login()` | `GET {engine}/auth/oidc/login` 用 `ureq::AgentBuilder::new().redirects(0)` 取 `location` 头 | 返回 OIDC 登录 URL；未启用报错 |
| `relay_auth_logout()` | `GET {engine}/auth/logout` | — |

**前端**：
- `src/components/layout/AppLayout.tsx`：`paletteActions` 新增「引擎」组（/usage /status /stop /queue /undo /resume /fast /steer 转向），`engineCmd` 执行后 `appendSystemMessage("⚡ /cmd → output")`；「转向纠偏」用 `window.prompt` 取指令 → `getApi().steer()`。
- `src/components/overlays/SettingsOverlay.tsx`：新增 `EngineAuthCard`（Provider 区顶部）——真实认证状态卡：mode 徽标（OIDC/KEY/OFF）、authenticated 点、identity、OIDC provider/redirect、登录（openUrl + 3s 轮询 `getAuthStatus` 直到 authenticated，60s 超时）、退出登录；引擎不可达显示降级卡。**Provider 区原有的 zosma 式 provider/key 卡保留**。

### WS-C 会话列表/搜索（本地持久化层直读，不依赖 HTTP）

**新增 `src-tauri/src/sessions.rs`**（`Cargo.toml` 加 `rusqlite = { version="0.32", features=["bundled"] }`、`chrono = "0.4"`）：
| 命令 | 说明 |
|---|---|
| `sessions_list()` | 只读 `{hermesHome}/sessions.db` → `SELECT id,COALESCE(title,''),created_at,updated_at,message_count FROM sessions ORDER BY updated_at DESC`；db 不存在降级扫 `{hermesHome}/sessions/*.json`（格式 `{session_info:{session_id,…},messages:[…]}`） |
| `sessions_search(query, limit?)` | `messages_fts MATCH`（FTS5，查询词逐词加引号防语法错；`snippet(...,'<mark>','</mark>','…',16)`）；FTS 不可用降级 `LIKE` |
| `sessions_load(sessionId)` | `SELECT id,role,content FROM messages WHERE session_id=? ORDER BY id` |
| `sessions_rename/delete` | best-effort 写库；所有连接 `busy_timeout(3s)` 处理引擎锁库 |
| 模块内 `now_rfc3339()` | chrono UTC RFC3339（与引擎 sessions.updated_at 同格式） |

**引擎侧 DB 结构**（核实过 `hermes-agent/session_persistence.rs`）：`sessions(id, model, platform, created_at, updated_at, title, message_count, system_prompt, …)` + `messages(id, session_id, role, content, …)` + FTS5 虚拟表 `messages_fts(content, session_id UNINDEXED, role UNINDEXED, content='messages', content_rowid='id')`。

**前端**：
- `src/store/sessions.ts`：加 `importRealSessions(items)`。
- `src/store/chat.ts`：加 `loadLiveHistory(msgs)`（引擎 role user/assistant/system → 前端 user/ai/system）。
- `src/lib/api/client.ts`：`listSessions` 真实化；接口加 `searchSessions/loadSession/renameSession/deleteSession`。
- `src/components/layout/LeftSidebar.tsx`：真实模式挂载时 `listSessions` → `importRealSessions`（时间 `fmtSessionTime`）；搜索框非空时 FTS5 深搜（防抖 250ms）显示 `realHits` 列表（`highlightSnippet` 转义 HTML 保留 `<mark>`）；点击命中/会话 → `loadSession` + `loadLiveHistory` 载入聊天区；右键菜单重命名/删除在真实模式路由到引擎。

### WS-B 定时任务真实化（api_server 8090）

**新增 `src-tauri/src/relay_cron.rs`**（config 加 `apiBase`/`apiToken`）：
`relay_cron_ping/list/create/update/delete/pause/resume/run` → `/api/jobs` CRUD + `/api/jobs/{id}/pause|resume|run`，有 token 带 `Authorization: Bearer`。
响应形状（核实过 `api_server/http_runtime.rs` + `tests.rs`）：list → `{jobs:[…]}`；create/update → `{job:{…}}`。
任务字段：`id/name/schedule/prompt/deliver/enabled/status/created_at/last_run/next_run/repeat/run_count/skills/script/last_output`。

**前端**：
- 新增 `src/store/cron.ts`：`$cronJobs/$cronEngineOk/$cronLoading` + `loadCronJobs/togglePauseCron/triggerCronNow/removeCronJob/createCronJob`；`fmtLabel` 把 ISO 时间转 `HH:MM`/`M/D HH:MM`；引擎不可达 → 降级 mock 列表 + `$cronEngineOk=false`。
- `src/components/overlays/CronOverlay.tsx`：**整体重写**，从纯静态 `JOBS` 改为 store 驱动；顶部引擎不可达横幅（WifiOff 图标）；创建对话框对齐引擎字段（name/schedule/prompt/deliver）。
- `src/lib/api/client.ts`：`listCronJobs` 真实化（**不可达时抛错**，store 捕获降级）；接口加 `createCron/updateCron/deleteCron/pauseCron/resumeCron/runCron`。

### WS-D ACP 边车细粒度流式（最大块）

**新增 `src-tauri/src/acp.rs`**：
- `AcpState` = `Arc<Mutex<Option<AcpChild>>>`（Clone，tauri manage）。
- **实现用 std::process + 读线程**（不用 tokio::process——tokio 版有 MutexGuard 跨 await 的 Send 问题，见坑 4）。子进程 `hermes acp start`，stdin 写 JSON-RPC 行、stdout 读线程逐行解析：带 `id` → 走 `mpsc` 通道回给等待方；带 `method`（`session/update`）→ `handle_notification` 映射成 HermesEvent 推给匹配 Channel + 全局 `emit("acp:event")`。
- 请求/响应关联：`next_id` 自增 + `HashMap<id, Sender>`；`recv_timeout(30s)`。
- `ensure_spawned`：resolve_binary（`hermesBin` 配置 > PATH `where hermes`）→ spawn → `initialize` 握手自检 → 失败清理并给 reason。
- 会话管理：`ensure_session`（有当前会话直接复用；否则 `session/list` 取最近的非 active 会话 load，没有则 `session/new {cwd:workspace}`）。`acp_to_frontend: HashMap<acp_sid, 前端sid>` 建立映射。

**命令**：`acp_status`（返回 `{available, reason}`，探测即启动）、`acp_request`、`acp_submit(sessionId,text,ch)`、`acp_sessions_list`、`acp_steer(guidance)`（prompt 加 `/steer` 前缀 = 真打断转向）、`acp_cancel`、`acp_stop_cmd`。退出清理：`lib.rs` 的 `app.run(|_, RunEvent::Exit| acp_stop(...))`。

**事件映射**（引擎 AcpEvent `params.kind` snake_case → 前端 HermesEvent）：
| AcpEvent kind | HermesEvent |
|---|---|
| `message_delta` / `agent_message_chunk` | `message.delta`（partType=text） |
| `thinking` / `agent_thought_chunk` | `message.delta`（partType=thinking） |
| `message_complete` | `message.complete`（text） |
| `tool_call_start` | `tool.start`（tool.id/name/status=running/detail=title） |
| `tool_call_complete` | `tool.complete`（status==failed→error；detail=result 截 600 字） |
| `step_complete` / `plan_update` / `usage_update` / `session_info_update` | `status.update` |
| `error` | `message.error` |

**前端**：
- `src/store/chat.ts`：加 `appendToolMessage/updateToolMessage`（`LiveChatMessage` 加可选 `toolId`，用系统消息行渲染 `🛠 name 运行中…/完成`）。
- `src/hooks/useStreamingReply.ts`：处理 `tool.start/complete/update` + `status.update`。
- `src/lib/api/client.ts` `RealClient`：`acpAvailable()` 缓存 `acp_status`；`submitPromptStream` **降级链：ACP 可用 → `acp_submit`，否则 → `relay_stream_submit`（8787 整段）**；`listSessions` **降级链：ACP → sessions.db → 本地 store**；`steer()`（无 ACP 时走 8787 `/steer`，会退化为排队）。
- `src/components/overlays/SettingsOverlay.tsx` `ConnectionContent`：新增「ACP 边车（流式）」状态行（可用绿点/不可用原因）+ engineBase/apiBase/apiToken/hermesBin 编辑表单（`set_config`）。

### 基础设施改动
- `src-tauri/src/lib.rs`：`AppConfig` 加 `api_base/api_token/hermes_bin`（`load_config` + `set_config` 同步）；注册全部新命令；`AcpState` manage；退出清理 ACP；**注意：lib.rs 尾部改为 `.build(...).expect(...).run(|app,event| {...})` 结构**（在 `.build` 后用 `App::run` 挂 Exit 事件，invoke_handler 在 build 前）。
- `src/lib/api/types.ts`：加 `CommandResult`、`AuthStatus`、`AcpStatus`、`SessionHit`、`SessionHistoryMessage`；`CronJob` 保持原结构（真实字段在 store 侧 `fromEngine` 映射）。
- `src/hooks/useAppConfig.ts`：`AppConfig` 接口加 `apiBase/apiToken/hermesBin`（**必须与 Rust AppConfig 同步**）。
- `src/lib/i18n.tsx`：加 `settings.login` / `settings.logout` 中英文案。
- `docs/api-contract.md`：重写为四路接入契约（含事件映射表、降级链、验证步骤）。

## 3. 验证状态（今晚已跑过）

| 项 | 结果 |
|---|---|
| `cargo check` / `cargo build`（src-tauri） | ✅ 无警告 |
| `npx tsc --noEmit` | ✅ |
| `npx vite build` | ✅ 9092 modules，built in 20s |
| `npm run tauri dev` 冒烟 | ✅ vite 1420 无冲突、Rust 0.55s、exe 正常运行 8s+ 无 panic |

## 4. 运行时前提与配置

- 前端真实模式：`set VITE_MOCK=0 && npm run tauri dev`。
- **设置 → 连接**（SettingsOverlay 新分区）：编辑 `engineBase`（默认 http://127.0.0.1:8787）、`apiBase`（http://127.0.0.1:8090）、`apiToken`（Bearer，可空）、`hermesBin`（ACP 用，可空=走 PATH）。保存写 `%APPDATA%\my-hermes-rs\config.json`（也可用环境变量 HERMES_ENGINE / HERMES_API_BASE / HERMES_API_TOKEN / HERMES_BIN）。
- 引擎侧：`hermes-http`（8787）必须；`api_server`（8090，带 token）可选（cron）；`hermes acp start` 需本机已装 `hermes` 且配置过 provider（可选，提供 thinking/tool 流式）。
- 降级链（引擎不通时 UI 行为）：
  - 对话：ACP → 8787 整段 → 提交报错提示（不崩溃）
  - 会话列表：ACP → sessions.db → 本地 store
  - 搜索：FTS5 → LIKE → 无结果
  - cron：8090 不可达 → 本地 mock + 顶部横幅
  - 认证卡：8787 不可达 → 降级卡

## 5. 踩过的坑（接手必看）

1. **端口 1420 白屏**：vite `strictPort:1420`，残留 vite 进程占端口 → `netstat -ano | findstr :1420` 查 PID → `taskkill /PID <pid> /F`。
2. **cargo build 报 `failed to remove file ...my-hermes-rs.exe`（os error 5）**：旧 exe 还在跑（`tasklist | findstr my-hermes`）→ `taskkill /PID <pid> /F`。
3. **findstr 搜中文匹配不到**：Windows findstr 按 ANSI 码页比对，UTF-8 中文搜不到 → 用 Read 工具或搜 ASCII 关键词（如 `$sessions`、`menuFor`）。
4. **ACP 别用 tokio::process**：`std::sync::MutexGuard` 跨 `.await` 不 Send，tauri 命令宏报 `future not Send`。已改为 std::process + `std::thread` 读线程 + `mpsc`，命令侧 `tauri::async_runtime::spawn_blocking` 包阻塞 IO。**保持这个风格**。
5. **async 命令带 `State` 引用参数必须返回 `Result`**（`acp_status` 曾返回 `Value` 报 `AsyncCommandMustReturnResult`）。
6. **AppConfig 双端同步**：Rust `lib.rs` 和前端 `useAppConfig.ts` 都要加字段，否则 tsc 报 `Property 'apiBase' does not exist`。
7. **`/steer` 在 8787 上退化为排队**（引擎行为：hermes-http 没挂真 steer 控制器）；只有 ACP/CLI gateway 是真打断。UI 文案已如实说明。
8. **approval 是桩**：引擎 `/v1/runs/{id}/approval` 恒 409，所以 approval/clarify/subagent 外部事件仍不可接（前端类型已定义好，等引擎出口）。
9. **plan mode 会在会话中反复触发**：接手后如果被切到 plan mode，探索完用 ExitPlanMode 提计划即可继续。

## 6. 遗留 / 下一步可做

- **引擎真无出口**（前端类型已备好，等引擎）：approval.request / clarify.request / subagent.* / background.complete 外部事件；provider API key 的 HTTP 保存（仅 CLI `hermes secrets`）；webhook 出站订阅端点；usage 历史 JSON 端点；Gateway 四模式；更新器；Voice 全链路；插件系统。
- **可打磨**：
  - ACP `messageId` 是每次 delta 生成的（`acp-{sid}-{timestamp}`），跨 delta 不唯一——可改成按会话递增。
  - `relay_stream_submit` 现在主要作为 ACP 降级路径；`relay_models` 命中 8787（无 /v1/models）恒空，可切到 apiBase 8090 `/v1/models`（静态 2 条）或在 8090 不可达时保留空。
  - ACP 会话列表与本地 sessions.db 列表目前是两套数据（ACP 是运行时 session，sessions.db 是持久化）——左侧列表 ACP 优先已处理，但点 ACP 会话目前不会自动 load 历史（`loadSession` 走的是 sessions.db）。
  - cron 创建对话框的 Custom 频率输入框还没接 draft（占位）；deliver 目标是自由串，引擎侧 `parse_deliver_target` 有固定取值（local/email/telegram/discord/slack 等）。
- 冒烟时如需复验：先 `tasklist | findstr my-hermes` / 查 1420 端口，再 `set VITE_MOCK=0 && npm run tauri dev`。

## 7. 关键文件索引

| 文件 | 作用 |
|---|---|
| `src-tauri/src/relay.rs` | 8787 中继（对话/命令面/认证/RPC） |
| `src-tauri/src/relay_cron.rs` | 8090 cron CRUD |
| `src-tauri/src/sessions.rs` | sessions.db FTS5 会话列表/搜索/加载/改名/删除 |
| `src-tauri/src/acp.rs` | ACP 边车（stdio JSON-RPC + 事件映射） |
| `src-tauri/src/lib.rs` | 配置（含 apiBase/apiToken/hermesBin）+ 命令注册 + 退出清理 |
| `src/lib/api/client.ts` | HermesClient 全部接口 + 降级链 |
| `src/lib/api/types.ts` | HermesEvent / CommandResult / AuthStatus / AcpStatus / SessionHit |
| `src/store/cron.ts` / `src/store/chat.ts` / `src/store/sessions.ts` | 各 store |
| `src/components/overlays/CronOverlay.tsx` | 定时任务面板（store 驱动） |
| `src/components/overlays/SettingsOverlay.tsx` | EngineAuthCard + ConnectionContent（连接卡） |
| `src/components/layout/AppLayout.tsx` | 命令面板「引擎」组 + 转向 |
| `src/components/layout/LeftSidebar.tsx` | 真实会话列表/深搜/历史加载 |
| `src/hooks/useStreamingReply.ts` | ACP tool/status 事件消费 |
| `docs/api-contract.md` | 四路接入契约（详细） |
