# 工作交接 — 2026-08-12（全天）：Hermes 原型 S3 系列落地 + 六模式环境 + 弹窗化改造

> 给下一位接手 AI 看的交接文档。本文覆盖 **今天（2026-08-12）全部** 在 `C:\Users\Administrator\my-hermes-rs`
> （React 19 + Vite + Tailwind v4 + Tauri v2 + Rust 桌面客户端）的工作区改动——依据文件修改时间（08-12）
> 而非仅最近一段会话。按时间分两个阶段：
> **阶段一（白天）**：Hermes 原型 S3 系列页面与基础设施大批落地；**阶段二（晚）**：六模式环境 + 弹窗化改造（用户报告的三个 bug 修复）。
> 前置背景见 `HANDOFF-2026-08-11.md`（四路引擎接入）。git 分支 `1`，今日改动均未提交。

---

## 0. 一句话总结

今天把 Hermes Dashboard 原型从"引擎接入"推进到 **S3 系列页面全面落地**：
**① 知识星空图（d3 力导向 + 时间轴 + HML1 分享码）② 插件系统骨架（注册表 + 贡献点 + 示例插件）③ 引擎连接/首次运行引导全流程（gateway 状态机 + onboarding + 四类覆盖层）④ 控制台/预览面板 ⑤ 会话多标签条 + 按会话 mock 聊天 + 线程内查找 ⑥ 可重绑定快捷键 / TTS / i18n 中英切换 ⑦ 右侧工具栏 + 右栏面板定义共享**；
随后晚间完成用户报告的三个修复：**⑧ 六模式档案落成真实 UI ⑨ 知识星空图改弹窗显示 ⑩ 左侧栏"新窗口打开"改为应用内对话框（外观与子对话栏完全一致）⑪ 项目(hermes-finance) 超长文字单行省略**。

---

## 1. 今日改动明细（按功能域）

### 阶段一（白天）：S3 原型页面与基础设施

#### A. 知识星空图（Starmap）— S3-5

| 文件 | 内容 |
|---|---|
| `src/components/starmap/StarmapView.tsx` | d3-force 力导向星空图。节点：hub / session / project / plugin；含时间轴联动 |
| `src/components/starmap/StarmapTimeline.tsx` | 48 桶直方图：横轴=会话创建时间（左旧右新），桶高=该时间段会话数；播放 reveal 0→1 渐进点亮节点；点按/拖动 scrub 定位任意时刻 |
| `src/components/starmap/ShareControls.tsx` | 分享（生成 HML1 分享码，可复制）/ 导入（粘贴重建星图，校验失败给原因）/ 重置（清除导入，恢复本地 store 快照） |
| `src/lib/share-code.ts` | HML1 分享码编解码 |

> 晚间将其从全页改为 **弹窗显示**（见阶段二 C）。

#### B. 插件系统（骨架）

| 文件 | 内容 |
|---|---|
| `src/plugins/registry.ts` | 插件注册表 + 贡献点 |
| `src/plugins/samples/hello.tsx` | 示例插件：演示顶栏工具下拉菜单 + 独立页面路由两个贡献点 |
| `src/store/plugins.ts` | 插件 store |
| `src/components/overlays/PluginsOverlay.tsx` | 插件面板 |

#### C. 引擎连接 / 首次运行引导全流程

| 文件 | 内容 |
|---|---|
| `src/store/gateway.ts` | 引擎连接状态机 |
| `src/store/onboarding.ts` | 首次运行引导 store |
| `src/components/layout/OnboardingGate.tsx` | 引导门（未完成引导前的入口） |
| `src/components/overlays/OnboardingOverlay.tsx` | 引导面板 |
| `src/components/overlays/GatewayConnectingOverlay.tsx` | 引擎连接中覆盖层 |
| `src/components/overlays/BootFailureOverlay.tsx` | 启动失败覆盖层 |
| `src/components/overlays/FirstRunRemoteForm.tsx` | 首次运行远程连接表单 |
| `src/components/overlays/DesktopInstallOverlay.tsx` | 桌面端安装提示覆盖层 |

#### D. 控制台 / 预览面板

| 文件 | 内容 |
|---|---|
| `src/store/console.ts` | 日志环形缓冲 store |
| `src/store/preview.ts` | 预览目标 store（`$previewTarget`） |
| `src/components/layout/ConsolePanel.tsx` | 控制台面板 |
| `src/components/layout/PreviewPanel.tsx` | 预览面板 |
| `src/components/chat/status-stack/PreviewRow.tsx` | 预览链接行：读 `$previewTarget`（默认空→不渲染），点击新标签打开 |

#### E. 会话多标签 + 聊天基建

| 文件 | 内容 |
|---|---|
| `src/store/open-tabs.ts` | 会话多标签（$openTabIds） |
| `src/components/session/SessionTabs.tsx` | 会话多标签条（S3-4）：打开/关闭/切换/拖拽排序；点击标签→setActiveSession（主区跟随、侧栏高亮同步）；关活跃标签激活右邻居；全关显示空态 |
| `src/components/session/SessionSwitcher.tsx` | 会话切换器 |
| `src/store/session-chat.ts` | 按会话 mock 聊天（晚间被 SessionDialogOverlay 复用） |
| `src/store/chat.ts` | 实时聊天消息 store（VITE_MOCK=0 时使用） |
| `src/components/chat/InThreadFind.tsx` | 线程内查找 |
| `src/components/chat/MessageLocator.tsx` | 消息定位器组件 |
| `src/components/chat/status-stack/StatusStack.tsx` | 活动状态聚合内容 |
| `src/components/chat/tool-call/ToolEntry.tsx` | 单个工具调用行 |
| `src/components/chat/tool-call/TerminalOutput.tsx` | 工具调用终端输出 |

#### F. 快捷键 / 工具 / 多媒体

| 文件 | 内容 |
|---|---|
| `src/lib/keybinds.ts` | 可重绑定快捷键 |
| `src/lib/tts.ts` | Web Speech 语音 |
| `src/lib/input.ts` | 输入模拟工具：`simulateRightClick`（在坐标派发 contextmenu 事件，浏览器/vite 环境测试用） |
| `src/lib/generated-images.ts` | 生成图像数据层 |
| `src/components/chat/GeneratedImage.tsx` | 生成图像展示 |
| `src/components/chat/DiffusionCanvas.tsx` | 扩散画布 |

#### G. i18n / 配置 / 排程 / 覆盖层复刻

| 文件 | 内容 |
|---|---|
| `src/lib/i18n.tsx` | 轻量国际化（默认中文，支持 English 切换，localStorage 持久化；`useI18n()` → `t("settings.model")` 点号路径） |
| `src/hooks/useAppConfig.ts` | 消费 Tauri `config` 命令 |
| `src/lib/constants.ts` | API_BASE（`http://127.0.0.1:8787`）等常量 |
| `src/store/cron.ts` | 排程任务 store |
| `src/components/overlays/CronOverlay.tsx` | 排程面板（对齐原型 Cron） |
| `src/components/overlays/SettingsOverlay.tsx` | 设置面板（文案走 i18n 中英切换） |
| `src/components/overlays/SkillsOverlay.tsx` | 技能与工具面板（按原型 Skills & Tools 精确复刻） |
| `src/components/overlays/ProfilesOverlay.tsx` | 档案页（对齐原型 Profiles） |
| `src/components/overlays/WebhooksOverlay.tsx` | Webhooks 面板 |

#### H. 右侧栏 / 工具栏 / 视图页

| 文件 | 内容 |
|---|---|
| `src/components/layout/RightToolbar.tsx` | 右侧工具栏（60px × 815px，顶栏下方） |
| `src/components/layout/rightTabs.ts` | 右侧栏面板定义（主应用与覆盖层弹窗共用——事件载荷只能传可序列化数据，图标组件映射放这里共享） |
| `src/components/layout/RightSidebar.tsx` | 右侧栏（380px × 880px） |
| `src/pages/ViewPages.tsx` | 左工具栏视图专属页（code 复用文件查看器真实浏览；其余轻量占位，接后端后替换） |
| `src/components/command-palette/CommandPalette.tsx` | 顶部命令搜索下拉（对齐原型 command-palette） |

#### I. API 客户端抽象 / 流式

| 文件 | 内容 |
|---|---|
| `src/lib/api/client.ts` | `HermesClient`：MockClient（VITE_MOCK=1，默认，不发网络请求）/ RealClient（VITE_MOCK=0，经 Tauri relay.rs 转发引擎） |
| `src/lib/api/types.ts` | 对齐 hermes-agent-main tui_gateway JSON-RPC 协议子集（session.* / prompt.submit / message.* / model.* / skills.* / cron.*），完整契约见 `docs/api-contract.md` |
| `src/hooks/useStreamingReply.ts` | 消费流式回复事件（参考 zosma-cowork usePiStream）：`submitPromptStream(sessionId, text)` 经 Tauri Channel 提交，事件流写入聊天 store |
| `src/hooks/useGitStatus.ts` | 轮询工作区 Git 状态（CodingRow 数据源）：复用 `check_git_workspace`（branch/added/removed/ahead/behind），15s 轮询；非 Tauri 静默失败 |

#### J. 既有组件今日打磨（mtime 08-12，多为配合上述功能）

`App.tsx`（deep-link 监听）、`TopBar.tsx`、`LeftToolbar.tsx`、`MainPanel.tsx`、`Composer.tsx`、`MemberChatPanel.tsx`、`MarkdownText.tsx`、`LeftSidebar.tsx`、`AppLayout.tsx`、`index.css`、`store/sessions.ts`、`store/projects.ts`（左侧栏项目树数据源，本地持久化，接后端后由 project.* 同步）。

### 阶段二（晚）：六模式环境 + 弹窗化改造（用户报告 bug 修复）

#### K. 六模式档案设计（本阶段核心概念）

对应原型"配置档案"：**6 个相互独立的 Hermes 环境，各有独立配置/技能/SOUL.md**，由左工具栏图标切换：

| 入口 | 环境 | 项目文件夹（会话列表"项目"归属） | 侧栏标题 | 团队 |
|---|---|---|---|---|
| logo（H 按钮） | Hermes 主环境（全能个人助理） | `hermes` | Hermes | Hermes 主环境团队 |
| 聊天 | Chat Hermes | `hermes-chat` | Chat Hermes | 聊天智能体团队 |
| 代码 | Code Hermes | `hermes-code` | Code Hermes | 代码智能体团队 |
| 工作 | Work Hermes | `hermes-work` | Work Hermes | 工作智能体团队 |
| 金融 | Finance Hermes | `hermes-finance` | Finance Hermes | 金融智能体团队 |
| 写作 | Write Hermes | `hermes-write` | Write Hermes | 写作智能体团队 |

**关键设计决策（用户确认）**：切换模式 = 切换环境身份（侧栏团队名/项目文件夹联动），**主对话区始终保持对话**，不替换成专属页。

#### L. 六模式 UI 落地

- **`src/components/layout/AppLayout.tsx`**：`ViewId` 新增 `"hermes"`；默认视图 `useState<ViewId>("hermes")`（启动即进主环境）；`handleViewChange` 中 OVERLAY_BY_VIEW 命中→开 overlay，否则 setActiveView + 展开左栏；深链 `?sessionId=` 读取并激活会话。
- **`src/components/layout/LeftToolbar.tsx`**：logo 按钮（85px 区，`h-10 w-10` 圆形 "H"）`onClick={() => onViewChange("hermes")}`，title="Hermes 主环境"。
- **`src/components/layout/LeftSidebar.tsx`**：`viewConfigs` 新增 `hermes` 条目（label "Hermes"、initials "HM"、avatarBg "#303030"、desc "全能个人助理 · 六模式之根 · 全局协作"）。
- **`src/components/layout/MainPanel.tsx`**：`viewPage` 逻辑——6 个 Hermes 环境（hermes/chat/code/work/finance/write）→ `null`（保持对话区）；仅工具类视图（bookmarks/knowledge 等）渲染专属页。

#### M. 项目文件夹名随模式切换 + 超长省略

**`src/components/layout/LeftSidebar.tsx`**：
- `viewFolder` 计算（cfg 定义后）：`activeView === "hermes" ? "hermes" : "hermes-${activeView}"`。
- 两处渲染替换（原 hardcode "hermes-chat"）：
  - 项目折叠区标题：`项目（{viewFolder}）`，**外层 span 加 `min-w-0 flex-1 truncate` + `title` 悬停提示**（修复 hermes-finance 两行换行问题）。
  - 项目目录行：`{viewFolder}-{proj.name.replace(/\s+/g, "")}`。

#### N. 知识星空图 → 弹窗显示

**`src/components/layout/AppLayout.tsx`**：`OverlayView` 新增 `"knowledge"`；`OVERLAY_BY_VIEW` 新增 `knowledge: "knowledge"`；lazy 新增 `StarmapOverlay`；渲染 `{overlayView === "knowledge" && <OverlayShell title="知识星空图" width={1100} height={760}>…<StarmapOverlay />…}`。点知识库图标 → 弹窗显示星空图，主对话区保持。

#### O. "新窗口打开" → 应用内对话框（外观与子对话栏完全一致）

- **`src/store/session-dialog.ts`（新建）**：`$sessionDialog = atom<string | null | undefined>(undefined)` + `openSessionDialog(id)` / `closeSessionDialog()`。`undefined`=关闭，`null`=空白新会话（⌘⇧N）。
- **`src/components/overlays/SessionDialogOverlay.tsx`（新建，重构）**：fixed 居中浮层 + 遮罩，内部**直接复用 `MemberChatPanel`**：
  - 会话包装成 ConvItem（name=会话标题、initials=标题前两位、avatarBg #303030、desc "会话对话"）。
  - 消息转 `ChatMessage[]`（ai/system → member，user → user）。
  - 打开时 `setActiveSession(resolvedId)`（Composer 发送写入正确会话）。
  - 发送：Composer 已写用户消息，本组件 500ms 后追加模拟 AI 回复。
  - 关闭：遮罩点击 / Esc / MemberChatPanel 标题点击。
  - **为什么复用 MemberChatPanel**：用户要求"新窗口应该和子对话栏一模一样"，复用保证 Header/消息气泡/Composer/终端完全一致，零样式漂移。
- **`src/lib/sessionWindow.ts`（重构）**：`openSessionWindow` 原为 Tauri `invoke("open_session_window")` 新开 WebviewWindow / 浏览器 `window.open` 新标签页 → 改为 `openSessionDialog(sessionId)` 应用内弹窗。**不再开新软件窗口**。
- **`src/App.tsx`**：⌘⇧N 仍调 `openSessionWindow(null)`（现在弹空白新会话对话框）。
- **`src-tauri/src/window.rs`**：`open_session_window` 命令保留未删（前端不再调用），如需可清。

---

## 2. 涉及文件清单（今日 mtime 08-12）

**新建（untracked）**：
`src/store/session-dialog.ts`、`src/store/console.ts`、`src/store/cron.ts`、`src/store/gateway.ts`、`src/store/onboarding.ts`、`src/store/open-tabs.ts`、`src/store/plugins.ts`、`src/store/preview.ts`、`src/store/projects.ts`、`src/store/session-chat.ts`、
`src/lib/sessionWindow.ts`、`src/lib/keybinds.ts`、`src/lib/tts.ts`、`src/lib/input.ts`、`src/lib/share-code.ts`、`src/lib/generated-images.ts`、
`src/hooks/useGitStatus.ts`、`src/hooks/useStreamingReply.ts`、
`src/components/overlays/SessionDialogOverlay.tsx`、`BootFailureOverlay.tsx`、`DesktopInstallOverlay.tsx`、`FirstRunRemoteForm.tsx`、`GatewayConnectingOverlay.tsx`、`OnboardingOverlay.tsx`、`PluginsOverlay.tsx`、`WebhooksOverlay.tsx`、
`src/components/layout/ConsolePanel.tsx`、`OnboardingGate.tsx`、`PreviewPanel.tsx`、`RightToolbar.tsx`、
`src/components/session/SessionTabs.tsx`、`src/components/starmap/StarmapView.tsx`、`StarmapTimeline.tsx`、`ShareControls.tsx`、
`src/components/chat/InThreadFind.tsx`、`DiffusionCanvas.tsx`、`GeneratedImage.tsx`、`tool-call/TerminalOutput.tsx`、
`src/plugins/registry.ts`、`src/plugins/samples/hello.tsx`、`src/pages/ViewPages.tsx`

**修改（tracked）**：`src/App.tsx`、`src/components/layout/AppLayout.tsx`、`LeftSidebar.tsx`、`LeftToolbar.tsx`、`MainPanel.tsx`、`TopBar.tsx`、`RightSidebar.tsx`、`rightTabs.ts`、`src/components/chat/Composer.tsx`、`MemberChatPanel.tsx`、`MessageLocator.tsx`、`markdown/MarkdownText.tsx`、`status-stack/StatusStack.tsx`、`tool-call/ToolEntry.tsx`、`src/components/command-palette/CommandPalette.tsx`、`src/store/chat.ts`、`sessions.ts`、`src/lib/constants.ts`、`i18n.tsx`、`api/client.ts`、`api/types.ts`、`src/index.css`

**Rust（mtime 08-12，主体见 08-11 交接文档）**：`src-tauri/src/{relay,acp,sessions,terminal,files,git,config,window,relay_cron,lib}.rs`、`Cargo.toml`。今日具体增量：`window.rs` 确认前端不再调用 `open_session_window`；其余多为 08-11 深夜工作延续到今日凌晨的收尾（详见 `HANDOFF-2026-08-11.md`）。

---

## 3. 验证状态

- **tsc**：`npx tsc --noEmit` EXIT=0（全项目，覆盖阶段一+阶段二代码）。
- **浏览器回归（localhost:1420，VITE_MOCK=1）阶段二全部通过**：
  1. 刷新默认进 Hermes 主环境：侧栏标题 "Hermes"、团队"Hermes 主环境团队"、项目 `（hermes）`。
  2. 六模式切换：侧栏标题 + 项目文件夹逐一对应（chat→hermes-chat，code→hermes-code，…，write→hermes-write）。
  3. 主对话区始终保持（输入框/消息/搜索框在）。
  4. 知识星空图弹窗：标题/图例/分享/导入/播放时间轴齐全；遮罩点击关闭；主区保持对话。
  5. 会话右键"在新窗口打开"→ 应用内对话框：项目上下文徽标/消息气泡/Composer（添加菜单+模式切换+终端开关）/悬停操作栏齐全；遮罩关闭、标题关闭均正常；主对话区不受影响。
  6. 项目文字省略：scrollW(170) > clientW(162)，ellipsis 生效。
- **阶段一功能未经本次会话逐一回归**：白天各面板（插件/设置/技能/档案/排程/Webhooks/控制台/预览/命令面板）以编译通过 + 代码为据，未在浏览器逐一验证；接手后建议按上表逐项点验。

---

## 4. 注意事项 / 已知限制

1. **验证环境是浏览器 mock**（`VITE_MOCK=1`，localhost:1420）。真实引擎模式（`VITE_MOCK=0`）需在 Tauri 桌面端复测，尤其：
   - `openSessionWindow` 不再 invoke Tauri 新窗口 —— 若产品上仍想要"真·新窗口"，需重新走 `window.rs` 路线（当前前端已不调用）。
   - 会话对话框 `messages` 目前仅 mock 分支渲染（`MOCK ? … : []`），真实模式消息来源待接 `$liveMessages`/`relay:reply`。
   - `useStreamingReply` / `HermesClient(RealClient)` 依赖 Tauri Relay 与 hermes-agent-main 引擎，桌面端未验证。
2. **IAB 合成键盘不可靠**：浏览器自动化无法注入文本到 Composer 端到端验证发送，发送逻辑与主对话区/子对话栏同一代码路径（`appendSessionUserMessage`），行为一致。
3. **日志/前端技巧**：
   - 工具栏按钮 getByRole 点击有时超时 → 用 `tab.cua.click({x,y})` 坐标点击（工具栏 70px 宽，x=35；logo 中心 y≈42，chat y=85+24，code +48，work +96，finance +144，write +192）。
   - 侧栏头部标题点击（x≈126, y≈42）可在"团队列表 / 会话列表"两模式间切换 —— 项目折叠区只在会话列表模式显示。
   - `tab.playwright.evaluate` 只读；有副作用（focus/dispatchEvent）会被拒；`tab.playwright.reload` 不存在，用 `tab.goto(...)`。
   - Broker 偶发 "response id mismatch"：重新 `browser.tabs.get(id)` 恢复 binding。
4. **未提交**：当前分支 `1`，今日全部改动未 commit（含阶段一大量 untracked 文件）。提交前建议先跑 `npx tsc --noEmit`。
5. 相关记忆（`.zcode`）：`model-no-vision-browser-verify`（验证走 DOM/文本，不给模型看截图）、`my-hermes-rs-rust-relay-missing`（历史：交接文档声称的 Rust 中继此前曾缺失，本次已由 08-11 交接文档确认补齐）。

---

## 5. 下一步建议（供参考）

- 接入真实引擎时：会话对话框真实消息源（`$liveMessages`）、`useStreamingReply` 真链路、Tauri 深链会话窗口按需保留。
- `window.rs::open_session_window` 若确认不再需要可删，减少死代码。
- 六模式"独立配置/技能/SOUL.md"目前是展示层；真正按模式加载 profile 配置待引擎侧档案系统就绪后接入。
- 阶段一各覆盖层面板（插件/设置/技能/档案/排程/Webhooks/命令面板）建议在浏览器 mock 下做一轮逐项回归。
