# 工作交接 — 2026-08-27：深度审查修复 + 环境隔离 + effort 收敛

> 接续 `HANDOFF-2026-08-21.md`。当日前期：拉取家里 28 提交（dad03fc→3c89c35）并做三方深审
> （sidecar 精读 + 双代理扫前端/Rust）；项目从 C 盘迁至 **`G:\my-hermes-rs`**（C 盘目录已删，
> 引擎以 `vendor/deepseek-harness` junction 挂入软件目录，更新走「更新dsh核心.bat」，见记忆
> my-hermes-rs-g-drive-relocation）。本日提交 658c7dc → 445db6d，全部已推 Gitee master。

---

## 0. 一句话总结

按审查清单完成可修项全量收口（4 高 5 中若干低），并上线两大功能级修复：

1. **环境隔离**：左栏模式（主/代码/工作/金融写作…）= 工作环境，切换即换引擎工作区 +
   会话命名空间；对齐 dsh「会话持久化按 cwd 分组」的原生语义，零引擎改动。
2. **effort 三源收敛**：推理强度此前 UI 显示"中"、引擎跑 max、设置页是死路——现统一到
   `providerConfig.activeEffort` 单一真相源。
3. **历史回放打通**：工具行/压缩标记此前只在"死代码路径"里实现过，真实回放只出文本——
   已接通（loadLiveHistory 复活）。

## 1. 深审结论要点（含已修）

**高（全修）**
- Rust `scmd_r` 四条早退路径泄漏 pending 条目（注释与代码相反）；`send_prompt` 写失败不回滚
  → 统一移除。
- app 退出只清 ACP、不杀 sidecar 进程树 → Exit 分支 taskkill /T（DshRelayState 加 pid）。
- effort 三源打架（见 §2）。
- 回放断链 + history.ts TDZ 地雷（tool-call id fallback 引用未初始化数组，触发即整段历史读崩）。

**中（全修）**
- get_history/rpc 未先 loadSessionMap（重启后竞态空历史）；feedback 映射错归 currentSessionId。
- set_effort 立即 shutdownRuntime 会杀流式回合 → 延迟生效（ensureRuntime 重启键加
  effort/envId/cwd diff）。
- abort 死变量清除；clear_queue 对 prompt 条目补 error/done 信封（drain 返回 dropped）。
- 提问卡三洞：ok:false 不再吞卡（可重试）、6 分钟过期兜底清卡（引擎侧 5 分钟超时）、
  answerUserQuestion 只清匹配 rpcId 的批次。
- 审批链不可达 / 模型静默回退：见下"未修"与 status.update 说明。

**低（修了大部分）**
- RealClient listen 注册竞态（unlistenPromise）；default 桥 user_question 校验对齐 zosma 桥；
  空 messageId 写 finalizedIds（??→||）；deleteSession 清 contentSessions；MockClient 孤儿 delta；
  scripts/t*.txt 调试 dump 清除+gitignore；respawn 指数退避封顶 30s；
  sidecar 日志 printf 占位符求值（%s/%d/%j）。

## 2. 关键设计记录

- **环境=视图映射**：`envIdForView(view)`——hermes/chat→main，其余 view id 即环境 id。
  MainPanel 唯一切换流水线（串行，序号防旧包）：set_env → sync_provider_config →
  load_session → get_history(loadLiveHistory)。sessionMap 键 = `<envId>::<frontendId>`
  （旧裸键自动迁移 main）。cwd 空串=系统默认；首次切换 sidecar 自动建目录。
- **effort 链路**：设置页/Composer 都读写 `activeEffort`；Composer 初值从 effectiveEffortOf
  反查档位（无值=max，与引擎一致）、挂载推送 dsh_set_effort 对齐 sidecar；滑块松手写回 store
  + invoke。sidecar set_effort 只记录，下次 ensureRuntime diff 重启生效。
- **dsh_set_env 新命令**（Rust 校验 env_id/cwd 控制字符），`dsh.ts` 的 workspace()/setWorkspace()。

## 3. 验证状态

- 前端 tsc ✅ / sidecar tsc ✅ / cargo check ✅ / smoke-providers ALL PASS ✅
- 冷启动实测（tauri dev from G:）：窗口进程存活；sidecar 日志确认 set_env(main,CWD 用户主目录)
  生效、Composer 推送 effort=max 对齐、session map loaded(旧键迁移)、history 回放出消息。

## 4. 未修（明确 deferred）

1. **审批链产品化**（高价值 feature 级）：approval.request/tool.update/status.update 生产侧
   全缺，审批 UI 不可达。需调研 dsh 权限引擎的 JSON-RPC 面——独立任务。（模型静默回退提示已做：
   runOne 回退时发 status.update 状态气泡，default 档消费；minimal 桥未消费该事件。）
2. 统一事件 parser O-2（家庭 HANDOVER 已列）；zstd 切帧/get_history 同步解析（同前）。
3. uuid crate 替换时间戳 uuid_v4； SESSION_ID "main" vs $activeSessionId "default" 双键漂移
   （send_prompt 不带 sessionId 时无害，relay_submit 路径仍按 main 计）。
4. 家里 D:\deepseek-harness-master 的 sdk/server 提问桥接改动仍未补丁化：**那台机器上运行时
   无法干净同步官方更新**。待办=在家人机器导出 git diff 存 patches/ 并由「更新dsh核心.bat」重放。

## 5. 下一步建议

1. 手动验证链：切到「代码」模式发消息 → 看 G:\Workspaces\code 是否被创建、dsh 日志是否落在
   该 cwd 分组；改工作区路径后保存是否立即重载当前会话。
2. anthropic 消息端到端仍是"boot 通过、未真发消息"状态，需一次真实对话验证。
3. 设置页环境区目前只支持改名/路径；后续可加"每环境默认会话/独立历史视图"。


---

## 追加批次（同日晚）：五假点真化 + 最大化修复（c5df296）

用户指出五处假数据，全量真化：

1. **输入框多余 deepseek 组**：sidecar catalog id（小写）与内置表（大写）分裂两组 → provider 名归一映射后分组（PROVIDER_DISPLAY 表）。
2. **设置页「获取模型」假**：改为复用 relay_probe 真连端点（GET {baseURL}/models），ModelListEditor 加 endpoint prop 绑定表单连接字段；未填地址/密钥提示而非回落 mock。
3. **「所有会话」硬编码**：种子 s1~s7 真实模式自动清洗；sidecar list_sessions 升级（首条 user 消息做标题/mtime/frontendId/envId）；左栏并入当前环境引擎历史会话、点击经 load_dsh_session(dshSessionId) 采纳磁盘会话再回放；活跃空白会话置顶显示。
4. **技能与工具面板假**：TABS 只剩 skills/tools；技能 = skill.list RPC（生成配置启用 skill-filesystem+skill+tool-skill 且 agent-spine skills.enabled=true 正则改写）；mcp/hub/webhook/plugins/agents 假面板删除（待引擎能力接入回归）；工具徽标 = $toolCalls 计数。boot 探针验证五插件装载 OK。
5. **排程假**：dsh 无 cron RPC——官方语义是 schedule 插件的 session 内提醒工具（schedule_create/list/delete；after/at/every）。生成配置补 time-context+schedule（对齐官方 web-schedule overlay）。创建/删除/立即运行 = send_prompt 驱动模型调工具；登记表 localStorage v2 持久化；旧 api_server mock/降级假数据全部废弃。

**最大化按钮修复**：decorations:false+transparent:true 的 Windows WebView 下原生 toggleMaximize 会撑满盖任务栏且窗口不进系统最大化态——TopBar 手写：记原边界 → currentMonitor().workArea 物理像素 setPosition/setSize；capabilities 补 monitor/is-maximized 权限。

**边界语义记录**：
- dsh schedule 是 session-local：任务随创建时会话存活（UI 文案已注明）；
- skill.list 需会话存在（sidecar 传当前 sessionId）；扫描目录 ~/.dsh/skills 等；
- 模型发现已探明 llm.discoverModels RPC 面（apiproxy），本次用 relay_probe 直连等价实现，未来可切官方方法。

---

## 追加批次二（同日夜）：回访修复 + 团队卡真化 + 流式实测（f2b5998 / 335b34f / 2e4f4d9）

### A. 获取模型 & key 实证修复
- **leveldb 实锤**：用户 providerConfig 里 `apiKey` 字段存的是 baseURL 值、connected=false——
  key 从未存上，"获取模型"必然失败。
- `relay_probe` 重写：返回 `{ok,count,models:[{id,name}]}`（此前只返回 count——空失败的根因）；
  URL 拼接防 `/v1/v1` 双写；anthropic 系改发 `x-api-key`+`anthropic-version` 头。
- 存量脏数据清洗：providerConfig 载入时把 URL 形状 apiKey 清空并置未连接；
  EditProviderForm 直接回填真 key 到密码框可核对；persist 加 URL 形状防御。

### B. 错误显示人话化
adapter 把引擎错误翻译为中文场景句（402 欠费/401 密钥/403 权限/429 限流/超时/断网/
缺凭据/5xx），原始错误保留在 details 进控制台；retryable 同步判定。
错误链路清单（覆盖场景与显示样式）已与用户核对过一轮。

### C. 其余假点续清
- 输入框内置 deepseek 目录只在「完全没配 provider」时兜底出现（单提供商用户不再看到多余组）；
  分组前 PROVIDER_DISPLAY 归一大小写。
- 工作环境：种子补齐 write 视图环境；设置页新增「＋添加工作环境」（绑定空闲左栏模式）。
- 插件列表真化：sidecar 新增 `dsh_rpc config.pluginEntries`，解析生成的 cordis.yml 条目镜像返回
  （引擎无对应 RPC）；设置页插件列表消费之，空态区分未连接/无匹配。

### D. 团队列表与提供商下拉
- 左栏团队卡 "8 成员/24 对话" 字面量 → $agents 数 + 当前视图非归档有内容会话数。
- agents 演示种子（id1~6）仅 mock 构建；真实模式 load() 清洗落盘旧数据。
- 「添加提供方」下拉源从 providers.ts 7 条演示行（openai 假已连接）改为
  已配置 providerConfig + 内置六家兜底——**两层不同源是"对齐后又变回 7 个"的根因**。

### E. 回复慢实测结论（stream-probe 三轮时间线）
- 管道逐事件同步无缓冲（engine→sdk server→client→sidecar→Rust→Channel 全链确认）；
- **瓶颈=网关首包延迟 6.7s~17.6s 且方差大**（tokenrhythm 聚合行为，客户端不可缩短）；
  正文到达后 0.6~2.3s 内流完，无"整段憋住"。
- 体感修复三件套：
  1. adapter turn/start 即发 `🧠 模型思考中…` status.update、首个内容 chunk 切
     `✍️ 正在生成回复…`（前端降噪已存）；
  2. 默认 effort max→high（Composer EFFORT_TO_INDEX 未设置档/sidecar 初始/runtime 注释同步；
     max 是 dsh rc.8 模板演示值，放大首包等待）；
  3. 空窗空气泡占位：MainPanel 流式中最后一条 AI 消息无正文无 thinking 时渲染三点跳动 +
     "正在等待模型返回…"。

### F. 输入框用量真化
$usage（token-meter usage 事件累计）替代写死 96% 假进度条：输入/缓存读/推理/输出四段
真实占比 + 引擎调用次数；容量按模型目录 contextWindow 折算百分比；无调用显示引导文案。
CONTEXT_BREAKDOWN 假数组删除。

### G. 最大化 v3 下沉 Win32
v2（JS setPosition/setSize 读回校验+逻辑像素二次推满）实测仍只抖动换位不放大。
彻底方案：Rust 新命令 `toggle_main_maximize` —— windows crate 启用
Win32_UI_WindowsAndMessaging/Win32_Graphics_Gdi features，GetWindowRect 取原边界保存进
Mutex，SetWindowPos(HWND) 到 MONITORINFO.rcWork；再次点击还原。TopBar 琥珀点只调此命令。
**需要重启软件生效（Rust 改动）。**

### 验证状态（本批）
前端 tsc ✅ / sidecar tsc ✅ / cargo check ✅ / stream-probe 三轮取证 ✅（探针文件未入库，
取证过程记录于本节）/ 冷启动窗口进程与 sidecar ready ✅

### 未修（接续 deferred）
- 设置→插件配置页签仍是本地表单（引擎 settings.* 需插件注册 section 才可写；
  接口面已探明 settings.describe/update，待逐插件接线并标注"本地草稿"）；
- 统一事件 parser O-2；SESSION_ID "main" vs $activeSessionId 双键漂移；
- 家里机器 sdk/server 补丁仍未导出 patches/；
- relay_probe 对 anthropic messages 协议网关的 /models 兼容性需真实 anthropic key 验证一次。

### 命名候选（已给用户，待拍板后改名）
鲸枢 KunHub（推荐，DeepSeek 鲸鱼意象+枢纽）/ 弦外 Subtext / 夜航 NightHelm / 素问 Suwen。
改名范围：窗口标题、bundle 显示名、安装包名；**bundle identifier 不要动**
（动了会清 WebView 本地存储 = 密码/providerConfig/会话映射全丢）。
