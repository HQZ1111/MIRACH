# 工作交接 — 2026-08-21：dsh 简约对话全套移植 + 设置页通用分区 + anthropic-messages 支持

> 给下一位接手 AI 看的交接文档。覆盖今天（2026-08-21）在 `C:\Users\Administrator\my-hermes-rs`
> （React 19 + Vite + Tailwind v4 + Tauri v2 + Rust 桌面客户端）的全部改动，**19 个提交
> （4b11338 → 074d05d）全部已推 Gitee master，工作区干净**。
> 上一份交接见 `HANDOFF-2026-08-14.md`（中间 08-15~08-20 无交接文档，那几天核心结论见下）。
> 与 08-20 状态的关系：master 已回到家版外壳（24fb719），今天在其上做 dsh 单核心对齐。

---

## 0. 一句话总结

按之前计划（`ZCodeProject/.zcode/plans/plan-sess_*.md`）做完「通用设置分区 + 新建任务引导页 +
zosma 简约对话全套移植（dsh 引擎）」整条线，并收尾用户最关心的两件事：

1. **对话风格「简约」档 = 全套移植**：前端 zosma 组件树（`src/components/zosma/`）+ 后端
   dsh 引擎（新建 `agent-sidecar/` Node sidecar + `src-tauri/src/dsh_relay.rs` Rust 中继），
   左栏会话 ↔ dsh sessionId 映射续聊；默认档 hermes 引擎路径未动。
2. **设置页自定义模型真正可用**：`sync_provider_config` 把设置页 providerConfig 同步到
   dsh 后端，模型切换即驱动对话；最后打通 **anthropic-messages 协议**（用户网页上有
   anthropic，此前一直应用不了）—— 接入 dsh 官方 `llm-pi-ai` 插件，双适配器架构落地。

---

## 1. 改动明细（按时间线）

### A. 上午：Git 账户分区 + 通用输入弹窗（4b11338）

- 设置新增 **Git 账户**分区：改提交身份（name/email）/ 清除本地凭据。
- 新建 `PromptDialogOverlay.tsx` **通用输入弹窗**，替换此前所有「假 prompt」（`store/prompt-dialog.ts` 驱动），
  `LeftSidebar` 等入口统一走它。

### B. 中午：简约档全套移植（6911e00 / ee46bbd / 82dc082）

- **前端**（6911e00）：`src/components/zosma/` 全套从 zosma-cowork 移植（ChatView/ChatMessage/
  MessageInput/ThinkingBlock/ToolCallTimeline/AttachmentCard/FeedbackButtons/MarkdownComponents/
  CommandPalette/ConversationSearch/InThreadFind/FileMentionPopup/ArtifactPreview 等 50+ 文件），
  `minimal.css` 样式令牌；`src/lib/chat-width.ts`（820/1080/无限制三档，CSS 变量 `--chat-max-width`）；
  对话风格三档（默认/dsh系统/简约）存 `store/ui-settings.ts`，`MainPanel` 按档渲染组件树。
- **后端**（ee46bbd）：`agent-sidecar/`（Node JSONL stdin/stdout 中继，**全新目录**，依赖
  `@deepseek-ai/dsh-sdk-client`）+ `src-tauri/src/dsh_relay.rs`（`send_prompt/abort/steer/follow_up/
  clear_queue/get_models/set_active_model/load_session/list_sessions/sync_provider_config` 命令，
  `tauri::ipc::Channel` 流式推送）+ `src/components/zosma/hooks/usePiStream.ts` 事件适配层
  （dsh `session.event` → zosma 组件消费形状）。
- **会话集成**（82dc082）：左栏会话 ↔ dsh sessionId 映射（新会话自动建、历史续聊）；`ZosmaChat.tsx`
  适配器（MOCK 模式走 per-session store 不 invoke）。

### C. 下午：设置页打磨 + 对齐 dsh（089c468 → 70a3546，9 个提交）

- 输入框切档图标不变；dsh 用户气泡；设置滑块组件（089c468）。
- 简约档输入框 = 默认 Composer 套 zosma 玻璃样式（578ae6b）。
- 通用设置区对齐 dsh：权限三档、Agent 预设四档、令牌一体式下拉、语言双档、inset 描边；
  输入框跟随对话宽度；简约档聚焦光晕恢复 zosma 品牌蓝（7086f40）。
- 下拉框改**卡片一体展开**（CustomProviderRow 同款，切换不跳动，fe4c91c）。
- 模型分区：初始页 API key + 自定义模型；一体下拉紧凑化；**彻底删除宠物系统**（`src/components/pet/` 整个删，8113851）。
- 已设置模型列表 + 间距 + 底部描边（f1b617c）；下拉背景浅灰化、去白、入口卡/按钮去蓝（360dce9/066a7f9）。
- 对齐 dsh：模型目录含 deepseek-official、插件配置/列表两页签、智能体预设分区（e551f36）。
- 设置页**仅关闭按钮可关闭**（点空白/Esc 不再关，70a3546）。

### D. 傍晚：dsh 模型接入 + anthropic-messages（bc4eb36 → 074d05d，6 个提交）

- 已设置模型卡片改造：浅灰底 + 状态点 + 编辑/删除按钮，编辑弹窗无提供商下拉（bc4eb36）。
- **dsh 单核心对齐**（ef84328）：sidecar 模型目录换 `deepseek-v4-flash/pro`（对齐 dsh v4 目录）。
- **设置页自定义模型接入 dsh**（ce26dcf）：`sync_provider_config` 同步目录/端点/key，发送带
  provider/model；`usePiStream` 改读 `$providerConfig` 取 active 配置。
- **hook 改名**（9a91625）：`usePiStream.ts` → `useDSHStream.ts`（git mv，数据入口已是 dsh）。
- **anthropic-messages 协议可应用**（074d05d，本会话核心）：

  **双适配器架构**（`agent-sidecar/src/dsh.ts`）：
  - deepseek 模型 → `llm-deepseek` 插件（握手 route 固定 `deepseek-official`，OpenAI 兼容
    chat completions；自定义端点/key 经 `DEEPSEEK_BASE_URL`/`DEEPSEEK_API_KEY` env 注入）。
  - anthropic/openai/自定义端点 → `llm-pi-ai` 插件（`I:\deepseek-harness\packages\llm\llm-pi-ai`，
    支持 anthropic-messages/openai-completions/openai-responses 协议；catalog 自带 anthropic/openai 路由）。
  - providers dict 经 `DSH_LLM_PROVIDERS` env 注入；各 provider key 走 `DSH_PROVIDER_KEY_<ID>` env
    （profile.apiKeyEnv 指向它）；握手 route 映射 `routeFor(id)`：deepseek → deepseek-official，其余 → 自身 id。

  **动态 cordis.yml**（`agent-sidecar/src/runtime.ts` `writeRuntimeConfig`）：以
  `I:\deepseek-harness\examples\jsonrpc-agent\cordis.yml` 模板为底，字符串拼接追加 llm-pi-ai 条目
  `providers: !!js "JSON.parse(process.env.DSH_LLM_PROVIDERS ?? '{}')"`，写入
  `%USERPROFILE%\.hermes\dsh-sessions\cordis.generated.yml`，运行时经 `DSH_CORDIS_CONFIG` env 指向
  （runner.ts env 优先于 argv[2]）。

---

## 2. 涉及文件

- **新增（60）**：`agent-sidecar/` 全套（src/adapter|dsh|index|protocol|queue|runtime.ts、tsconfig、
  package*.json、`tests/smoke-providers.ts`）、`src-tauri/src/dsh_relay.rs`、
  `src/components/zosma/`（50+ 文件）、`src/components/overlays/PromptDialogOverlay.tsx`、
  `src/lib/chat-width.ts`、`src/store/prompt-dialog.ts`、`src/store/ui-settings.ts`、`src/assets/zosma-mark.png`
- **删除（1）**：`src/components/pet/Pet.tsx`（宠物系统整体移除）
- **修改（重点）**：`SettingsOverlay.tsx`、`ZosmaChat.tsx`、`ProviderConnectPanel.tsx`、`index.css`、
  `MainPanel.tsx`、`Composer.tsx`、`AppLayout.tsx`、`LeftSidebar.tsx`、`lib.rs`、`dsh_relay.rs`、
  `providerConfig.ts`、`useDSHStream.ts`、`agent-sidecar/src/{dsh,index,runtime}.ts`

## 3. 验证状态（全部 ✅）

- 前端 `npx tsc --noEmit` ✅（下班前刚跑，EXIT=0）
- `cargo check`（src-tauri/ 下）✅
- sidecar tsc ✅
- sidecar 回归测试：`I:\node-v22.23.2-win-x64\node.exe --import tsx tests/smoke-providers.ts`
  （需 `DSH_HARNESS_ROOT=I:\deepseek-harness`）——providers dict 构建/生成文件结构全过
- dsh runtime 真实 boot 两次 ✅：anthropic 路由（provider=anthropic, model=claude-sonnet-4-5）
  initialize 握手成功；默认 deepseek 路径不受 env 新增影响

## 4. 注意事项 / 已知限制

1. **dsh 引擎依赖本机环境**（未打包）：sidecar 需要独立 Node ≥22.23.2（`I:\node-v22.23.2-win-x64\node.exe`，
   不在系统 PATH）；runtime 需要 harness checkout `I:\deepseek-harness`（DSH_HARNESS_ROOT 可覆盖）。
2. **改 provider 配置必须重启 runtime**：cordis.yml/plugins 只在进程启动时读一次；
   `ensureRuntime` 重启键 = provider/id/baseURL/apiKey/providersJson（dsh.ts）。
3. **凭证走 env**：jsonrpc-agent 模板无 credentials 插件 → llm-pi-ai `resolveApiKey` 回退
   `launchEnvironmentOf(ctx)` 读 runtime 进程 env，env 注入才有效；llm-pi-ai 包在
   `examples\node_modules`（entry 解析路径），不在 harness 根 node_modules。
4. 内置 deepseek 模型 provider 统一为前端 id `deepseek`（非 deepseek-official），findModel 按前端
   provider id 匹配；get_models 前端尚未消费（Rust 已暴露，未来模型选择 UI 可用）。
5. 设置页模型分区「测试连接」浏览器 preview 是模拟（桌面走 `relay_probe` 真连）——08-14 遗留行为。
6. API key 仍明文存 localStorage `hermes.providerConfig.v1`（用户确认）。
7. 模型无视觉约束：UI 验证只用 DOM/文本，不用截图。
8. 登录页左侧蓝色 + PS 混合模式仅 embedded=false 生效（08-14 定稿，今天未动）。

## 5. 下一步建议

1. **真实发一条 anthropic 消息**验证端到端（用户网页上已有 anthropic messages 配置）：设置页
   确认 anthropic provider（协议 anthropic-messages + baseURL + key + 模型），切到该模型发消息；
   首次发送会触发 runtime 重启（config 变化检测，预期）。
2. 挂 dsh 会话列表：`list_sessions` 目前返回空数组（Phase 5 遗留），左栏仍是 my-hermes-rs 自己的会话 store。
3. `get_models`/`set_active_model` 前端未接 UI（Rust 已通），后续可做模型选择器消费。
4. 08-15~08-20 结论提醒：master=家版外壳+今天改动；`I:\deepseek-harness` 是 dsh 引擎 checkout
   （不要动）；Gitee master 是唯一同步远端（HTTPS 弹窗登录，无 SSH key）。
