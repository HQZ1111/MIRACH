# 工作交接 — 2026-08-13（白天）：登录页独立预览路由

> 给下一位接手 AI 看的交接文档。覆盖今天（2026-08-13）在 `C:\Users\Administrator\my-hermes-rs`
> （React 19 + Vite + Tailwind v4 + Tauri v2 + Rust 桌面客户端）的改动。
> 昨天全部工作见 `HANDOFF-2026-08-12.md`（白天 S3 系列）与 `HANDOFF-2026-08-12-night.md`（晚：聊天记录弹窗/首启引导/安装层移除/启动密码登录）。
> git 分支 `1`，改动未提交。

---

## 0. 一句话总结

把**启动密码登录页**从 `StartupGate` 内嵌组件抽成独立 `LoginPage` 组件，并新增 **`?win=login` 浏览器预览路由**——不需要设密码、不需要启动完整应用，直接打开 URL 就能单独打磨登录页 UI（HMR 即改即看）。用户下一步计划：**登录页与动画过渡页的视觉设计**（昨晚文档遗留事项，今天只铺好了独立编辑的入口，UI 尚未开调）。

---

## 1. 改动明细

### A. `src/components/layout/LoginPage.tsx`（新建）

原 `StartupGate.tsx` 内嵌的 `PasswordGate` + `StatusRow` 整体搬出为独立组件：

- `LoginPage({ preview })`：
  - **`preview = false`**（默认，StartupGate 复用）：行为与原来完全一致——真实密码校验（`verifyAppPassword`）、引擎探活（`getApi().ping()`）、ACP 检测（`acpAvailable()`）、版本（Tauri `getVersion()`，浏览器降级 0.1.0）；输对密码 `opacity→0` 淡出 380ms 后 `unlockApp()`。
  - **`preview = true`**（`?win=login`）：跳过真实检测，用演示数据渲染（引擎=已连接 / ACP=可用 / 版本 v0.1.0）；任意非空密码视为成功（淡出不调 `unlockApp`）。方便纯 UI 打磨时不被启动逻辑干扰。
- 布局不变：`inset-3 rounded-[40px]`（与软件卡片同尺寸圆角）+ 左品牌面板（深色渐变 + H logo + 标语 + 引擎/ACP/版本状态行）+ 右密码表单（锁图标输入框 + 解锁按钮 + 底部提示）。

### B. `src/components/layout/StartupGate.tsx`（修改）

删除内嵌的 `PasswordGate`/`StatusRow`，`locked` 阶段改为 `<LoginPage />`。`SplashGate`（连接动画）不动。启动流程行为零变化。

### C. `src/main.tsx`（修改）— 预览路由

沿用已有 `?win=quick-entry` 分流模式，新增：

```ts
const isLogin = new URLSearchParams(window.location.search).get("win") === "login";
// 分流：isOverlay → isQuickEntry → isLogin（渲染 <LoginPage preview />）→ 完整 App
```

`isLogin` 分支包 `React.StrictMode + I18nProvider`（不包 HapticsProvider，登录页不用）。

---

## 2. 涉及文件清单

- **新建**：`src/components/layout/LoginPage.tsx`
- **修改**：`src/components/layout/StartupGate.tsx`、`src/main.tsx`
- **删除**：无

## 3. 验证状态

- `npx tsc --noEmit` ✅ EXIT=0（改后跑过两次）
- vite 编译：`main.tsx` / `LoginPage.tsx` 模块转换 HTTP 200，产物含 `win=login` 分流与登录页文案
- 真实渲染验证（headless Edge + CDP 抓 DOM，`--no-first-run --disable-sync` 避开 Edge 首启弹窗）✅：
  - 打开 `http://localhost:1420/?win=login` 渲染完整登录页：H logo / Hermes Agent / 标语 / 引擎（已连接）/ ACP（可用）/ 版本 v0.1.0 / 密码输入框（type=password，占位"输入启动密码…"）/ 解锁按钮 / 底部提示
- 桌面主窗口（tauri dev）未做视觉回归——`?win=login` 是浏览器独立入口，主窗口路径 `StartupGate → LoginPage(preview=false)` 逻辑与重构前一致，tsc 通过，风险低

## 4. 注意事项 / 已知限制

1. **Edge headless 在验证时要加 `--no-first-run --disable-sync` + 独立 `--user-data-dir`**，否则首启同步弹窗盖住页面、`--dump-dom` 输出为空。CDP 抓 DOM 用 Node 22 原生 `WebSocket`（项目里没有 playwright/ws 依赖）。
2. **`?win=login` 是纯浏览器入口**：mock 模式下引擎/ACP 状态行是演示值（preview 分支写死），不代表真实引擎状态。
3. 登录页视觉设计**尚未开始**（用户原话计划：品牌面板配色、logo、转场动画、密码框交互、版本/引擎状态展示细化；连接动画与登录页之间的过渡衔接）。今天只铺了独立编辑入口。
4. 沿用昨晚已知限制：密码是 localStorage + SHA-256 加盐本地锁（非强安全）；启动页每次启动都要输入（dev HMR 刷新 AppLayout 时也触发，预期行为）。
5. **未提交**：分支 `1`，全部改动未 commit。

## 5. 下一步建议

- **登录页 / 动画过渡页视觉设计**（用户指定主线）：直接在 `http://localhost:1420/?win=login` 打磨，改 `LoginPage.tsx` 即改即看；`SplashGate`（连接动画）在 `StartupGate.tsx`，两者过渡衔接也在那。
- 可顺手：AppLayout 里 `PreviewConnecting` 与 `SplashGate` 逻辑相似可合并（昨晚遗留建议）。
