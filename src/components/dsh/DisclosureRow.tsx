/**
 * DisclosureRow 统一实现 → dsh-ui 版（dsh ui-primitives 移植，DshToolRow/CompactionRow 共用）。
 * 本文件仅 re-export，避免两份实现分叉。
 */
export { DisclosureRow, type DisclosureRowProps } from "@/dsh-ui/DisclosureRow";