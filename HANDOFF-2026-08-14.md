# 工作交接 — 2026-08-14：登录连接流程改造 + 登录页视觉定稿

> 给下一位接手 AI 看的交接文档。覆盖今天（2026-08-14）在 `C:\Users\Administrator\my-hermes-rs`
> （React 19 + Vite + Tailwind v4 + Tauri v2 + Rust 桌面客户端）的全部改动。
> 昨天工作见 `HANDOFF-2026-08-13.md`；昨晚见 `HANDOFF-2026-08-12-night.md`。
> git 分支 `1`，**全部改动未提交**（含 4 个未跟踪的新文件/目录）。

---

## 0. 一句话总结

登录页两件事收尾：

1. **连接流程改造**（用户指定要求）：
   - 「测试连接」真实走后端 —— 新增 Rust 命令 `relay_probe`，桌面环境直连 AI 供应商 `/v1/models`（OpenAI 兼容 Bearer / Anthropic x-api-key），浏览器 preview 降级标注「预览模拟」。
   - **测试连接成功不再直接出现「进入主页」**；点「保存」（写入配置 + 测试成功）后才出现绿色「进入主页」按钮，再点进入。
   - 新建统一配置源 `src/store/providerConfig.ts`：登录页保存的配置（baseURL/协议/API key 明文/模型列表/当前模型）与设置页「模型」分区读写**同一份**（首次设置即保存，重启不丢）。
   - 密码保持前端 localStorage 加盐 SHA-256（`hermes.password.v1`），未动。
2. **登录页视觉**：
   - 右侧水印图（filler-bg0）改为「外层非滚动容器 + inset-0 wrapper + object-cover」**四边全贴齐**。
   - 左侧面板**背景保持深蓝 #026CFE 不变**，启动图（qidongye.jpg）用 PS 浅色系混合模式**四层叠加**：`color-dodge`(100%) / `overlay`(55%) / `hard-light`(40%) / `lighten`(30%)，呈现「浅色中的浅色」发光效果。
   - **重要坑**：PS 的点光 `pin-light` 值在当前 Edge/WebView2 引擎**实测不支持**（setProperty/cssText/类规则全被丢弃，computed 回退 normal），已用 `hard-light` 近似替代（见 §4）。

---

## 1. 改动明细

### A. 真实后端探测 — `src-tauri/src/relay.rs` + `lib.rs`

新增 `#[tauri::command] relay_probe(base_url, api_key, protocol) -> Result<Value, String>`（`lib.rs` 已注册）：

- URL 拼接：base 已含 `/v1` → `{base}/models`；否则 `{base}/v1/models`。
- 认证：`protocol.starts_with("anthropic")` → `x-api-key` + `anthropic-version: 2023-06-01`；否则 `Authorization: Bearer`。
- 成功返回 `{ok:true, models:[id…], count}`（解析 OpenAI 兼容 `data[].id` 形状）；失败 `Err("HTTP <status> <reason> <200 字符响应摘要>")`，超时 30s（复用 `http(timeout_secs)` helper）。

### B. 统一配置源 — `src/store/providerConfig.ts`（新建，未跟踪）

```ts
interface ProviderConfig {
  id; name; kind: "builtin" | "custom"; connected: boolean;
  baseURL; protocol; apiKey; keyHint?; models: ModelRow[]; activeModelId?;
}
```
- localStorage key `hermes.providerConfig.v1`，**apiKey 明文落盘**（用户明确确认）。
- 载入时自动迁移旧 `hermes.providerProfiles.v1` + `hermes.providerKeys.v1`（keyHint）。
- 导出：`$providerConfig` atom / `getProviderConfig(id)` / `activeModelIdOf(cfg)` / `saveProviderConfig(cfg)` / `setProviderConnected(id, connected, keyHint?)` / `setActiveModel(id, modelId)` / `builtinProviderOptions()`。

### C. `src/components/layout/ProviderConnectPanel.tsx`（新建，本次大改）

- `probeConnection` 重写：桌面（`"__TAURI_INTERNALS__" in window`）→ `invoke("relay_probe", { baseUrl, apiKey, protocol })` 真实直连；浏览器 preview → 降级 mock（invalid/bad 模拟失败，其余 getModels 成功 + 状态行标注「预览模拟」）。
- `AddProviderForm` / `AddCustomProviderForm` 各加 `saved` state：`persist()` 额外写 `saveProviderConfig({...})`；`runTest(persistFirst)` 开头 `setSaved(false)`、`persistFirst && r.ok` 时 `setSaved(true)`；`resetTest()` 也清 saved。
- `SaveAndEnter`：`test === "ok" && saved` 才渲染绿色「进入主页」；否则显示 `[测试连接][保存]` 按钮组。测试成功只显示绿色状态行（无进入按钮）。

### D. `src/components/overlays/SettingsOverlay.tsx` — 模型分区真实读写

- `ModelContent` 主模型三件套（提供商下拉 / 模型下拉 / 应用按钮）改读 `providerConfig`：提供商下拉 = 已配置 providers（含自定义），模型下拉 = 该 provider 的 `models`，`apply()` 调 `setActiveModel` + flash「已应用 ✓」；显示 baseURL · protocol · 已连接标记；未配置时给提示。
- 新增 `SelectInputC` 受控下拉组件（options 带 label）。

### E. `LoginPage.tsx` — 视觉

- **右侧水印铺满**：右面板拆「外层 relative 非滚动容器 + 水印层 `absolute inset-0`（`w-full h-full object-cover opacity-10 z-20`）+ 内层滚动区」。坑：`inset-0` 不能直接放替换元素 img（会保持宽高比变方形）；`width:100%` 在滚动容器内按 clientWidth 解析会差 2px —— 必须挂外层非滚动容器。
- **左侧 PS 混合模式**：背景 `bg-[#026CFE]` 不变；图片四层 `absolute inset-0` 叠加，每层 `style={{ "mix-blend-mode": mode } as React.CSSProperties}` + 独立 opacity：

  | 层 | 模式 | opacity | 说明 |
  |---|---|---|---|
  | 1 | `color-dodge` | 100% | 颜色减淡：主发光层 |
  | 2 | `overlay` | 55% | 叠加：增强明暗对比 |
  | 3 | `hard-light` | 40% | 点光近似（pin-light 不受支持） |
  | 4 | `lighten` | 30% | 浅色：整体提亮 |

### F. `src/index.css` + `src/components/layout/LeftSidebar.tsx` — 下拉令牌与语义类

今日早些时候：新增 `--color-dropdown-*` 令牌（border/bg/hover/title/sub/icon/accent）+ `.dropdown-card` / `.dropdown-card-trigger` 系列语义类（@layer components）；侧栏「已置顶会话/项目」等 Collapsible 列表改消费同一套语义类。登录页下拉栏、侧栏统一由令牌驱动（改样式只动 index.css）。

### G. `scripts/` — 13 个 CDP 验证脚本（新建，未跟踪）

全部用 Node 22 原生 WebSocket + headless Edge（`--remote-debugging-port` 各不相同）+ CDP `Runtime.evaluate`，连 `http://localhost:1420`：

- `verify-connect-flow-v2.cjs`（**最新，主流程回归**，port 9354）：A1 测试成功无「进入主页」/ A2 保存后出现 + localStorage `hermes.providerConfig.v1` 落盘 / A3 改 key 重置 / B1-B5 主界面 Ctrl+K → 设置 → 模型分区与登录页同源 + 应用按钮持久化 `activeModelId`。
- `verify-login-overlay.cjs`（port 9352 附近）：右侧水印图四边对齐回归（wrap=panel 尺寸、top/bottom/left/right 全 true、objectFit cover）。
- 其余：`verify-login*.cjs` / `verify-connect*.cjs` / `verify-models.cjs` / `verify-panel.cjs` / `verify-protocols.cjs` / `verify-tokens.cjs` / `verify-chevron.cjs` / `measure-*.cjs`（早前迭代的阶段性验证，可留作参考）。

### H. `src/assets/`（新建，未跟踪）

- `qidongye.jpg`（1279×1235，深蓝色调，平均 RGB≈44,106,195）—— 左侧启动图。
- `filler-bg0.jpg`（3.9MB，浅色内容）—— 右侧水印图。

---

## 2. 涉及文件清单

- **新建（未跟踪）**：`src/store/providerConfig.ts`、`src/components/layout/ProviderConnectPanel.tsx`、`scripts/`（13 个 cjs）、`src/assets/`（2 张 jpg）
- **修改**：`src-tauri/src/lib.rs`、`src-tauri/src/relay.rs`、`src/components/layout/LoginPage.tsx`、`src/components/overlays/SettingsOverlay.tsx`、`src/index.css`、`src/components/layout/LeftSidebar.tsx`
- **删除**：无

## 3. 验证状态

- `npx tsc --noEmit` ✅ EXIT=0（多次）
- `cargo check` ✅（relay_probe 编译通过）
- CDP 验证 ✅（headless Edge，详见 §1G）：连接流程 A1/A2/A3 + B1-B5 全通过（含 localStorage 落盘字段 key/baseURL/protocol/models/activeModelId、设置页下拉同源、应用按钮持久化）；水印图四边对齐回归全 true
- 左侧混合模式 ✅：四层 computed `color-dodge / overlay / hard-light / lighten` 全部生效，面板背景 `rgb(2,108,254)`，图片 763×737 上下贴边
- dev server（vite :1420）保持运行，`?win=login` 可直接预览

## 4. 注意事项 / 已知限制

1. **`pin-light`（PS 点光）在本机 Edge/WebView 引擎实测不支持**：`el.style.mixBlendMode="pin-light"` / `setProperty` / `cssText` / 动态类规则四种方式全部静默丢弃（getAttribute("style")=null，computed 回退 normal），而 `overlay`/`color-dodge` 对照正常。Tauri 桌面走 WebView2 同引擎，**必须用支持值**，现用 `hard-light` 近似点光。
2. **React style 键用 kebab-case + `as React.CSSProperties`**：camelCase `mixBlendMode` 对 pin-light 值在 Edge 有静默丢弃（React 底层 `style[mixBlendMode]=v`），kebab-case 走 setProperty 路径更稳；tsc 需断言（CSSProperties 不认 kebab-case 键）。
3. 混合模式四层 opacity（100/55/40/30）是调好的默认，用户后续若要求「更淡/更亮」改 `LoginPage.tsx` 里数组的 `op` 即可。
4. **Edge headless 验证三件套**：`--remote-debugging-port` + 独立 `--user-data-dir` + `--no-first-run --disable-sync`；CDP 用 Node 22 原生 WebSocket（项目无 playwright/ws）。
5. 密码为 localStorage SHA-256 加盐本地锁（非强安全，沿用）。API key **明文**存 `hermes.providerConfig.v1`（用户确认）。
6. **未提交**：分支 `1`，全部改动未 commit；`scripts/`、`src/assets/`、`ProviderConnectPanel.tsx`、`providerConfig.ts` 均未跟踪，提交时需 `git add` 这些路径。
7. 桌面 tauri dev 有端口坑：改后端（relay.rs）后需重新 `cargo` 编译；1420 被占用时先 `netstat`+`taskkill` 清理再启动。

## 5. 下一步建议

- 用户刚看过左侧混合模式效果，可能继续微调（opacity / 换模式 / 增删层）—— 交接后若用户回来先说视觉反馈。
- 剩余老脚本（`verify-connect-flow.cjs` 等早前迭代版）可择机清理归档，主回归保留 `verify-connect-flow-v2.cjs` + `verify-login-overlay.cjs`。
- 若后续要支持 Anthropic 真实模型的「获取模型」下拉，`relay_probe` 返回的 `models` 列表可直接喂给 `AddProviderForm` 的候选模型（现有代码只用了 ok/count，models 列表暂未消费）。
