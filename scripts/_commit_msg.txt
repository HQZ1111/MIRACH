语音 P1（引擎侧）：装好 peer 与 0.3.3，但引擎 alpha.1 装不下 peer；bundles 已复原可工作状态

- 官方 CLI 装好：dsh-multi-model-provider@0.1.0-rc.19、@deepseek-ai/dsh-client-runtime@0.1.1-rc.2、
  dsh-realtime-voice 升到 0.3.3（进 profile dependencies）
- 发现：把 peer 放进 profile dsh.profile.bundles → 引擎装配失败（cannot create effect on inactive
  context，SDK initialize 阶段）；且 dsh-pocket 一旦进 bundles 也会打断引擎
  （failed to apply loader entry dsh-pocket: cannot get property "webServer" without inject）。
  bundles 已恢复为不含二者、实测 runtime ready 的可知良好集合（_restore_bundles.ps1）
- 根因假设与下一步：运行时自带引擎是 0.1.5-alpha.1，而 npm latest/next 已是 0.1.5-rc.1 —— peer 大概率
  需要 rc 线；先把运行时引擎升到 rc.1 再试（详见 docs/voice.md）
- 新增 docs/voice.md：语音实施记录（已落地项 / 阻塞点 / 工具清单 / 麦克风待重启复验）
- 新增工装：_set_bundles/_restore_bundles/_toggle_peer_bundle/_boot_verdict/_check_voice_routes/
  _probe_mic/_verify_archive/_prune_apply/_prune_and_boot/_dev_with_runtime_engine/_kill_dev_stack/
  _repair_runtime/_compare_runtime_prune/_add_voice_plugins/_finish_voice_plugins
