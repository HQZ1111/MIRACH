应用内依赖安装（照搬 hermes bootstrap-installer）：首装二选一 + 分阶段进度页

- src-tauri/src/bootstrap.rs：安装编排（bootstrap 事件通道 + 阶段协议 + 取消/重置 + 版本过期刷新）
- scripts/mirach-install.ps1：四阶段载荷 node/deps/sidecar/marker（纯 ASCII、逐行 flush、UTF-8）
- src/store/bootstrap.ts + src/components/setup/SetupFlow.tsx：选择页/进度页/成功页/失败页 + 远端引擎分支
- 打包：resources 带安装脚本与 agent-sidecar（dist/config/package.json），安装包不再需要 1.8GB 便携包
- dsh_relay：运行时解析 = 便携优先 / 应用内安装（调试构建跳过）/ 仓库回退；MIRACH_DSH_BIN 注入
- sidecar：引擎入口优先内置 node_modules（MIRACH_DSH_BIN → 内置 → 全局 npm）
- 真机首装验证修掉：\\?\ 前缀（Join-Path 报 drive 为空）、管道编码/缓冲（乱码+丢结果帧）、
  下载分支缺目录、安装根与 exe 目录同名导致便携误判；无结果帧时错误带输出尾部
- 质量门：lint 0/0、前端 9 测试、sidecar 18 测试、Rust 17 测试
